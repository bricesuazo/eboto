
const Button = ({ children }) => {
    return (
        <button className="w-fit px-4 py-2 bg-primary text-white font-bold rounded-md m-auto">
            {children}
        </button>
    )
}

export default Button